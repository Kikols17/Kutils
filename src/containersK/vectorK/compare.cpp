#include <stdio.h>
#include <vector>

using namespace std;


int main() {
    vector<int> vec = vector<int>();

    for (long long int i=0; i<1000000000; i++) {
        vec.push_back(0);
    }

    

    return 0;
}