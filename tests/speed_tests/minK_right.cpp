#include <bits/stdc++.h>

using namespace std;


int main(int argc, char const *argv[]) {
    int n = atoll(argv[1]);

    for (int i=0; i<n; ++i) {
        int a=1;
        int b=5;
        a = min(a, b);
    }

    return 0;
}